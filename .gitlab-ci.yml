image: oven/bun:1.2.11

pages:
  stage: deploy
  script:
    - bun install --frozen-lockfile
    - DOCS_BASE=/keylab/ bun run docs:pages
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
